import os
import sys
import nibabel as nib
import numpy as np
from PIL import Image
from itertools import product
import math
import glob
import csv
import pandas as pd

DATA = pd.read_csv('Datainfo.csv')
titles = list(DATA)
reduced_titles.append(titles[1])
reduced_titles.append(titles[2])

df = []

reduced_data.append(pd.DataFrame(DATA())

df.append(pd.DataFrame(reduced_data, columns = titles))
